/* for (var number = 0; number < 10; number++) {
    console.log(number);
} */

// target: display every elements of an array by for loop

/* var arr = [34, 52, 94, 84, 98, 24, 76, 27, 99, 54];
for (var i = 0; i < arr.length; i++) {
    var number = arr[i];
    console.log(number);
}    */

var items = ['bottle', 'pen', 'sunglass', 'paper', 'notebook'];
for (var i = 0; i < items.length; i++) {
    var item = items[i];
    console.log(item);
}